# Render frontend → Railway backend (no secrets)
# ZitFlow: set on Render service zitflow-web (see render.zitflow.yaml).
# Legacy: oosm-web / osm-ms-fe-1.

BACKEND_URL=https://oosm-api-production.up.railway.app
