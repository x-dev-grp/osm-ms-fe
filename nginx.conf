server {
  listen 80;
  server_name _;

  # Serve the Angular app beneath /osm/
  location /osm/ {
    # SPA fallback: if file not found, serve index.html
    try_files $uri $uri/ /osm/index.html;
  }

  # Cache hashed static assets aggressively
  location ~* ^/osm/.*\.(?:js|css|png|jpg|jpeg|gif|svg|ico|webp|woff2?)$ {
    expires 30d;
    add_header Cache-Control "public, max-age=2592000, immutable";
    try_files $uri =404;
  }
}
